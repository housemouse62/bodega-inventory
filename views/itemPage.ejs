<html>
  <head>
    <title><%= items.item_name %></title>
    <link rel="stylesheet" href="/stylesheets/style.css">
  </head>
  <body>
    <header class="main-header">
      <div class="logo-nav-form">
        <a href="/"><img class="small-logo" src="/images/EverybodyLogo.jpeg" alt="Everybody's Bodega logo"></a>
            <div class="nav-form">
              <nav class="main-nav">
                  <a class="main-links" href="/">All Categories</a>
                  <a class="main-links" href="/items">All Items</a>
              </nav>
              <%- include("../partials/form.ejs") %>
            </div>
      </div>
    </header>
    <h1><%= items[0].item_name %></h1>
    <ul><h3>Categories</h3>
      
      <% items.forEach((item) => { %>
        <h3><%= item.category_name %></h3>
        <% }); %>      
        <h4>$<%= items[0].item_price %></h4>      
        <h4>In Stock: <%= items[0].item_stock %></h4>
        <h5><%= items[0].item_sku %></h5> 
        <img src="/images/<%= items[0].image_url || 'placeholder.jpg' %>" 
     alt="<%= items[0].name %>"
     onerror="this.src='/images/placeholder.jpeg'">
        <a href="/items/<%= items[0].item_id %>/confirmDeleteItem?from=<%= from %>&categoryName=<%= categoryName %>">
          Delete Item</a>
        </form>
    </ul>
    <footer>
      <a href="/items/new">Add New Item</a>
      <a href="/">Home</a>
    </footer>
  </body>
</html>
