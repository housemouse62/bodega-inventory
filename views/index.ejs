<html>
    <head>
        <title><%= title %></title>
    </head>
    <body>
        <h1><%= title %></h1>
        <ul>
            <% categories.forEach((category) => { %>
                <h2><%= category.name %></h2>
                <a href="<%= `/category/${category.id}`%>">Open</a>
            <% }); %>
        </ul>
        <footer>
        </footer>
    </body>
</html>